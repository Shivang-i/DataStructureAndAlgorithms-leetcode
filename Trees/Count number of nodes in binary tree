// User function Template for Java

class Solution {

    // Function to count the total number of nodes in a binary tree
    public static int countNodes(Node root) {
        // If the current node is null, there are no nodes to count
        if (root == null) {
            return 0;
        }
        
        // Start with count = 1 for the current node
        int ctr = 1;

        // Recursively count nodes in the left subtree and add to count
        ctr = ctr + countNodes(root.left);

        // Recursively count nodes in the right subtree and add to count
        ctr = ctr + countNodes(root.right);

        // Return the total count
        return ctr;
    }
}
