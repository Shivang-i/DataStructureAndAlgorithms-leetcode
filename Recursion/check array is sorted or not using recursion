//this code will check array is sorted or not using recursion.
static boolean Sorted(int arr[],int index){
    if(index==arr.length()-1)
    return true;

    return arr[index]<arr[index+1]&&Sorted(arr,index+1);
}